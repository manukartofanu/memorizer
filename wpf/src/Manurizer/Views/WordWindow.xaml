<Window x:Class="Manurizer.Views.WordWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
		xmlns:sys="clr-namespace:System;assembly=mscorlib"
        xmlns:local="clr-namespace:Manurizer.Views"
		xmlns:ctrls="clr-namespace:Manurizer.Controls"
		xmlns:viewModels="clr-namespace:Manurizer.ViewModels"
        mc:Ignorable="d" Width="432" Height="656"
        Title="Manurizer" SizeToContent="WidthAndHeight" WindowStartupLocation="CenterOwner">
	<Window.DataContext>
		<viewModels:WordViewModel/>
	</Window.DataContext>
	<Window.Resources>
		<Style x:Key="HeaderStyle" TargetType="TextBlock">
			<Setter Property="Foreground" Value="{StaticResource MidnightBrush}"/>
			<Setter Property="FontWeight" Value="SemiBold"/>
			<Setter Property="FontSize" Value="20"/>
		</Style>
	</Window.Resources>
	<StackPanel Width="384" Margin="24">
		<TextBlock Style="{StaticResource HeaderStyle}">Spelling</TextBlock>
		<StackPanel Margin="0 12 0 0">
			<ctrls:TextEdit LabelText="Word" Text="{Binding Path=Name}"/>
			<Grid Margin="0 12 0 0">
				<Grid.ColumnDefinitions>
					<ColumnDefinition Width="*"/>
					<ColumnDefinition Width="*"/>
				</Grid.ColumnDefinitions>
				<ctrls:TextEdit Margin="0 0 8 0" LabelText="Word class" Text="{Binding Path=Class}"/>
				<ctrls:TextEdit Grid.Column="1" Margin="8 0 0 0" LabelText="Transcription" Text="{Binding Path=Transcription}"/>
			</Grid>
		</StackPanel>
		<TextBlock Margin="0 24 0 0" Style="{StaticResource HeaderStyle}">Meaning</TextBlock>
		<Grid>
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="*"/>
				<ColumnDefinition Width="*"/>
			</Grid.ColumnDefinitions>
			<ctrls:TextEdit Margin="0 12 8 0" LabelText="Guide word" Text="{Binding Path=GuideWord}" CharacterCasting="Upper"/>
			<TextBlock Grid.Column="1" Margin="8 0 0 0" TextWrapping="Wrap" Foreground="#747F8D"
					   FontSize="10" VerticalAlignment="Bottom">
				Helps you to find the right meaning when a word has more than one meaning
			</TextBlock>
		</Grid>
		<TextBlock Margin="0 4 0 0">Definition</TextBlock>
		<ScrollViewer VerticalScrollBarVisibility="Auto" Height="256">
			<ItemsControl Margin="4" ItemsSource="{Binding Path=MeaningList}">
				<ItemsControl.ItemTemplate>
					<DataTemplate>
						<StackPanel Margin="4">
							<TextBlock Margin="0 4 0 0">Text</TextBlock>
							<TextBox Height="48" Text="{Binding Path=Text}" TextWrapping="Wrap"/>
							<TextBlock Margin="0 4 0 0">Examples</TextBlock>
							<TextBox Height="64" Text="{Binding Path=Example}" TextWrapping="Wrap" AcceptsReturn="True"/>
							<Rectangle Margin="0 16 0 0" Height="2" Fill="Gray"/>
						</StackPanel>
					</DataTemplate>
				</ItemsControl.ItemTemplate>
			</ItemsControl>
		</ScrollViewer>
		<Button Margin="0 4 0 0" Command="{Binding Path=DefinitionAddCommand}">Add Definition</Button>
	</StackPanel>
</Window>
